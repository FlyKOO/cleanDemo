<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 存储权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- 查询所有包权限 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- 获取应用使用统计权限 -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <!-- 设备管理员权限（可选，用于高级清理功能） -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />
    <!-- NEW permissions -->
    <!-- Android 13+ 媒体读取权限（尽可能涵盖媒体类大文件） -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- 兼容旧版本 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!-- 为了列出所有已安装应用（Android 11+ 的包可见性约束） -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>
    <!-- 使用 StorageStatsManager 统计各应用占用（需要用户在设置里授予“使用情况访问”） -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"/>

    <!-- 允许长期持有 SAF 权限（persistable）不需要声明额外权限 -->
    <application
        android:name=".CleanupApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cleanup"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Cleanup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".test.AppUsageStatsActivity"
            android:exported="true"/>
        <activity
            android:name=".test.LargeFilesActivity"
            android:exported="true"/>
        <activity android:name=".test.FixBig"
            android:exported="true"/>
        <!-- 定时清理服务 -->
        <service
            android:name=".service.ScheduledCleanupService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>